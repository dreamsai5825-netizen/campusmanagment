rules_version = '2';

// TEMPORARY: Permissive rules for development and initial implementation.
// Replace with proper auth- and role-based rules before production launch.

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
